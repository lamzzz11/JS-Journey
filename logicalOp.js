// logical operators = used to combine or manipulate  boolean values
//                     (true or false)
//                      AND = &&
//                      OR = ||
//                      NOT = !

const temp = 200;
if(temp > 0 || temp <=30) { // mending gini dari pada else if(biar mudah)
    console.log("the weather is good");
} 
else {
    console.log("the weather is BAD");
}